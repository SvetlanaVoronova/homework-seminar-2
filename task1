# Напишите программу, которая принимает на вход вещественное число
# и показывает сумму его цифр. Пример: - 0,56 -> 11

number = input('Введите только вещественное число: ')
total = 0
for i in number:
    if i != '.':
        total += int(i)
print(total)
